# 後決め事項 質問リスト

アーキテクチャで未確定の項目をまとめる。回答を追記していく。

---

## 1. フロントエンド状態管理・Context

| # | 質問 | 回答 |
|---|------|------|
| 1.1 | React の Context の持たせ方（グローバル vs ページ単位 vs 最小限）は？ | |
ページ単位でいい

| 1.2 | 状態管理ライブラリを使うか？（例: Redux, Zustand, Jotai）使う場合どれ？ | |
検討してほしい　シンプルなのがいい（学習コスト最優先）

| 1.3 | MPA なのでページ間で状態を共有しない前提でよいか？共有が必要なケースはあるか？ | |
ない。ある場合はlocalstorage保存するか。

| 1.4 | 認証状態（ログイン情報など）はどこで保持する？（Context / Cookie / 毎回API） | |
Cookie

---

## 2. Linter / Formatter

| # | 質問 | 回答 |
|---|------|------|
| 2.1 | Linter / Formatter はどれを採用する？（Biome / ESLint v9 + Prettier / oxlint + oxfmt） | |
biomeで
※ 詳細は `linter.md` 参照

---

## 3. スタイリング・UI

| # | 質問 | 回答 |
|---|------|------|
| 3.1 | CSS の手法は？（Tailwind / CSS Modules / styled-components / Vanilla Extract / 素のCSS / その他） | |
1ファイル内で完結させたい。tailwindはCSSすら怪しい人が使うもんじゃない


| 3.2 | UI コンポーネントライブラリを使うか？（MUI, Chakra, Radix, shadcn/ui 等） | |
一番簡単なのがいい

| 3.3 | デザイントークン（色・スペーシング等）の管理方法は？ | |

1ファイルで管理したい
---

## 4. 認証・認可

| # | 質問 | 回答 |
|---|------|------|
| 4.1 | 認証方式は？（Forms Auth / JWT / Cookie-based / 外部OAuth） | |
Cookieかな

| 4.2 | ASP.NET MVC と React 間の認証トークン受け渡し方法は？ | |
Cookieをヘッダに渡すのかなたぶん

| 4.3 | 未認証時のリダイレクト先・エラー表示方針は？ | |
未定

---

## 5. API 設計

バックエンドはすでにあるので考えなくていい

| # | 質問 | 回答 |
|---|------|------|
| 5.1 | API のレスポンス形式の統一方針は？（成功/失敗の形式、日付フォーマット等） | |
| 5.2 | エラーレスポンスの形式は？（HTTPステータス + JSON の構造） | |
| 5.3 | API のバージョニング方針は？（URL / Header / なし） | |


| 5.4 | フェッチライブラリを使うか？（fetch そのまま / axios / TanStack Query 等） | |
いったんfetchそのままで
cookieからとってきてheaderにのせてfetchする共通ファイルはいるかな？


---

## 6. フォーム・バリデーション

| # | 質問 | 回答 |
|---|------|------|
| 6.1 | フォームバリデーションは React 側のみ / API 側のみ / 両方？ | |
| 6.2 | フォームライブラリを使うか？（React Hook Form, Formik, なし） | |

なにそれあったほうがいいの？

---

## 7. テスト

| # | 質問 | 回答 |
|---|------|------|
| 7.1 | フロントエンドのテスト方針は？（単体 / 統合 / E2E） | |
| 7.2 | テストランナー・フレームワークは？（Vitest / Jest / Playwright / Cypress） | |
| 7.3 | バックエンド（C#）のテスト方針は？ | |
| 7.4 | テストカバレッジの目標はあるか？ | |

いったん考えない



---

## 8. 環境・設定

| # | 質問 | 回答 |
|---|------|------|
| 8.1 | 環境変数の管理方法は？（.env / Web.config / その他） | |
| 8.2 | 開発 / ステージング / 本番の切り替え方法は？ | |
| 8.3 | API ベースURL の切り替えは環境ごとにどう行う？ | |

---

## 9. 国際化・アクセシビリティ

| # | 質問 | 回答 |
|---|------|------|
| 9.1 | 多言語対応（i18n）は必要か？必要な場合、ライブラリは？（react-i18next 等） | |
| 9.2 | アクセシビリティ（a11y）の対応方針・レベルは？ | |
ない


---

## 10. セキュリティ

| # | 質問 | 回答 |
|---|------|------|
| 10.1 | CSRF 対策の方針は？（AntiForgeryToken 等） | |
| 10.2 | XSS 対策（CSP, サニタイズ等）の方針は？ | |
| 10.3 | 機密情報（APIキー等）の管理方法は？ | |
ない
---

## 11. パフォーマンス・ビルド

| # | 質問 | 回答 |
|---|------|------|
| 11.1 | コード分割・遅延ロードの戦略は？（Vite の dynamic import 活用範囲） | |
| 11.2 | 画像・静的アセットの最適化方針は？ | |
| 11.3 | 本番ビルドの最適化オプション（minify, tree-shaking 等）のポリシーは？ | |

よくわからない

---

## 12. 開発・運用

| # | 質問 | 回答 |
|---|------|------|
| 12.1 | CI/CD の構成・ツールは？（GitHub Actions, Azure DevOps 等） | |
| 12.2 | デプロイ先は？（IIS オンプレ / Azure / AWS 等） | |
| 12.3 | ログ・モニタリングの方針は？ | |
| 12.4 | エラーレポート（Sentry 等）を使うか？ | |

IIS まだ考えなくていい
---

## 13. その他

| # | 質問 | 回答 |
|---|------|------|
| 13.1 | ルーティング（React Router 等）は使うか？MPA なので不要の可能性あり | |
不要

| 13.2 | SSR / SSG の検討は？（現状は CSR のみの想定） | |

ssrするならcshtmlでやらないとだめでしょ。

| 13.3 | 共通レイアウト（ヘッダー・フッター・ナビ）の実装方針は？ | |
| 13.4 | ローディング・スケルトン表示の方針は？ | |
| 13.5 | 他に決めておくべき事項はあるか？ | |
